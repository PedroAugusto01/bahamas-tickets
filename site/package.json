{
  "name": "discord-auth-site",
  "version": "1.0.0",
  "description": "Site com autenticação Discord para acesso a vídeos.",
  "main": "server.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production node server.js",
    "dev": "cross-env NODE_ENV=development node --watch server.js"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "dotenv": "^17.2.2",
    "ejs": "^3.1.10",
    "express": "^4.18.2",
    "express-session": "^1.17.3",
    "session-file-store": "^1.5.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "cross-env": "^10.0.0"
  }
}
